{
  "name": "invizion-invoice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && tsc && cp index.html dist/index.html && cp src/styles.css dist/styles.css"
  },
  "dependencies": {},
  "devDependencies": {}
}
